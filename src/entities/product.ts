type Product = {
    id: number
    name: string
    description: string
    weight: string
    quantity: number
    value: number
    promotionalValue?: number
    image?: string
    enabled: boolean
}

export type { Product }
